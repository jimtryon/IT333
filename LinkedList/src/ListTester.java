import java.util.*;

/**
 * Created by jimtryon on 1/13/15.
 */

public class ListTester {
    public static void main(String[] args) {
        /*
        System.out.println("This program creates and uses a list.");

        List<String> friends = new LinkedList<String>();

        friends.add("John");
        friends.add("Brian");
        friends.add("Danielle");
        friends.add("Ryan");
        friends.add(2, "Samantha");

        System.out.println(friends.get(3));

        for (int i = 0; i < friends.size(); i++) {
            System.out.println(friends.get(i));
        }

        System.out.println();
        // debug code
        System.out.println(friends);

        System.out.println("-------------------------");
        // demo of an "Iterator"
        Iterator<String> itr = friends.iterator();

        while (itr.hasNext()) {
            System.out.println(itr.next());
        }

        itr = friends.iterator(); // go back to beginning

        System.out.println("---------------------------");
        // foreach loop (for every string in the friends list, do some action)
        for (String s : friends) {
            System.out.println(s);
        }
        */
    }
}